<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.figueroaluis.dormdash">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ProfileOptionFoodItem"
            android:label="@string/title_activity_profile_option_food_item"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".MyAdapter"
            android:label="@string/title_activity_my_adapter"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MyListActivity"
            android:label="@string/title_activity_my_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SearchScreen" />
        <activity android:name=".OrderScreen" />
        <activity android:name=".FragmentLogSign" />
        <activity android:name=".FragmentDetailedMarketplace" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>